{{- if .Values.migrations.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "cmul8.fullname" . }}-migrate-{{ .Release.Revision }}
  labels:
    {{- include "cmul8.labels" . | nindent 4 }}
  annotations:
    helm.sh/hook: pre-install,pre-upgrade
    helm.sh/hook-weight: "-10"
    helm.sh/hook-delete-policy: before-hook-creation
spec:
  backoffLimit: {{ .Values.migrations.backoffLimit }}
  activeDeadlineSeconds: {{ .Values.migrations.activeDeadlineSeconds }}
  template:
    metadata:
      labels:
        {{- include "cmul8.labels" . | nindent 8 }}
        app.kubernetes.io/component: migrations
    spec:
      enableServiceLinks: false
      restartPolicy: Never
      serviceAccountName: {{ include "cmul8.serviceAccountName" . }}
      automountServiceAccountToken: false
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: migrations
          image: {{ include "cmul8.image" . | quote }}
          args: ["migrations"]
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          env:
            {{- include "cmul8.env" . | nindent 12 }}
          volumeMounts: [{name: tmp, mountPath: /tmp}]
          resources:
            {{- toYaml .Values.resources.migrations | nindent 12 }}
      volumes:
        - name: tmp
          emptyDir: {sizeLimit: {{ .Values.temporaryStorage.sizeLimit }}}
{{- end }}
